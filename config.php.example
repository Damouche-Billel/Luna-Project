<?php
/**
 * LUNA Newsletter Configuration
 */

// Brevo API credentials
define('BREVO_API_KEY', '<brevo_api_key_here>');
define('BREVO_API_PASSWORD', '<brevo_api_password_here>');

// Database configuration - MySQL
define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'Luna');
define('DB_USERNAME', 'root');

// PRODUCTION (Comment/uncomment as needed)
define('DB_PASSWORD', '<production_db_password_here>');

// DEVELOPMENT (Comment/uncomment as needed)
define('DB_PASSWORD', '<development_db_password_here>');

function getDBConnection() {
    $conn = new mysqli(DB_HOST, DB_USERNAME, DB_PASSWORD, DB_NAME);
    
    if ($conn->connect_error) {
        error_log("Database connection failed: " . $conn->connect_error);
        return false;
    }
    
    return $conn;
}

function closeDBConnection($conn) {
    if ($conn) {
        $conn->close();
    }
}
?>
